# Supabase seeding
#
# 1. Create a project at https://supabase.com
# 2. Copy .env.example → .env and fill URL + service role key
# 3. In Supabase SQL Editor, run:
#      supabase/migrations/001_init.sql
# 4. Seed data (pick one):
#      A) npm run seed
#      B) paste supabase/seed.sql into the SQL Editor
#
# Seed emails all use @seed.pawssafe.local
# Shared demo password: Password123!
